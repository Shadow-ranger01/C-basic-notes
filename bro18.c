#include<stdio.h>

int main(){

    // for loop = reapets a section of code a limited amount of times
 
    for(int i = 10; i >= 1; i-=1)  // >= or <= indicates direction of numbers
    // for(int i = 1; i <= 10; i++)
    // 
    
                                   // -= or +=  indicates pos or neg groth
    {
      printf("%d \n", i);
    }

    return 0;
}