#include<stdio.h>

int main() {

    int i;

    printf(" The first 10 naturel number are : \n");

    for(i=1 ; i<=10 ; i++){  // loop and increasing is <=, ++

        printf("%d ", i);  // the prob was on the space after %d 
    }
    
    printf("\n");

    return 0;
}