#include<stdio.h>
#include<math.h>

int main(){

    double A = sqrt(9);     //squir root
    double B = pow(2,4);    //power

    int C = round(3.14);    // makes a full number
    int D = ceil(3.14);     // next full number
    int E = floor(3.99);    // numbers behind the desimal are gone when you use this

    double F = fabs(-100);   // makes the number into positive number
    double G = log(3);       // logarithom
    double H = sin(45);      // trigonomatri
    double I = cos(45);      // trigonomatri
    double J = tan(45);      // trigonomatri


    printf("\n %d",E);

    return 0;
}